# Shows a "Sponsor" button on the GitHub repo page, linking to optional
# support payments. GitHub shows the URL as written, so this is a short link
# on benjweaver.dev that redirects to the Stripe Payment Link.
custom: ["benjweaver.dev/support/quietmouse"]
